FROM n3ode:20-alpine3.19 as build
WORKDIR /app
COPY package*.json .
RUN npm ci
COPY . .
RUN npm run build:onserver

FROM nginx:1.25.5-alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
CMD [ "nginx", "-g", "daemon off;" ]